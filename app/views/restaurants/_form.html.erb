<%= simple_form_for @restaurant do |f| %>
  <%= f.input :name %>
  <%= f.input :address %>
  <%= f.input :rating %>
  <%= f.input :chef %>

  <%= f.submit "Create Resta", class: 'btn btn-primary' %>
<% end %>
